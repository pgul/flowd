- documentation
- change listen addr/port on reread config if needed (with and without
  receiver process)
- drop privileges after bind()
- netflow v9
- "snap" option in config line
- put extra info in snap (pkts, proto, ports)
- reloading perl filter: delete functions before reload
